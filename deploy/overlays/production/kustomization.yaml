apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: demo-service-production
resources:
  - ../../base

images:
  - name: ghcr.io/stonetusker/tusker-demo-notification-service
    newName: ghcr.io/stonetusker/tusker-demo-notification-service
    newTag: main

replicas:
  - name: demo-service
    count: 3

configMapGenerator:
  - name: demo-service-config
    behavior: merge
    literals:
      - DEMO_FAILURE_MODE=none
      - FAILURE_DELAY_MS=2500

labels:
  - pairs:
      app.kubernetes.io/environment: production

commonAnnotations:
  tuskerblueprint.io/release-sha: main